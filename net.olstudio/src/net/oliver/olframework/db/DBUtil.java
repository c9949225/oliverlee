package net.oliver.olframework.db;

import java.sql.Connection;

public class DBUtil {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		
		Connection connection = null;
		/*
		Class.forName(GlobalConfig.getInstance().getValue("OracleDriver"));

		String connURL = GlobalConfig.getInstance().getValue("OracleURL");

		String userName = GlobalConfig.getInstance().getValue(
				"OracleUserName");

		String password = GlobalConfig.getInstance().getValue(
				"OraclePassword");

		connection = DriverManager.getConnection(connURL, userName,
				password);*/

	}

}
